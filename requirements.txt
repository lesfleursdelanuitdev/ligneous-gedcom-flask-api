# Direct dependencies (human-maintained), pinned to the versions validated in the
# flask-api image. The full transitive set is locked in requirements.lock, which is
# what the Docker build installs. To bump: edit a pin here, rebuild, then regenerate
# the lock with `podman run --rm localhost/flask-api:dev pip freeze`.
Flask==3.1.3
gunicorn==26.0.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
groq==1.5.0
jellyfish==1.2.1
pytest==9.1.1

# Graph + tabular analytics (pedigree introspection, aggregates, quick charts)
networkx==3.6.1
pandas==3.0.3
matplotlib==3.11.0
plotly==6.8.0
